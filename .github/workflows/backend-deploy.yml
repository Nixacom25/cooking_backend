name: Backend CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: ./mvnw clean package -DskipTests

    - name: Log in to Docker Hub
      if: github.event_name == 'push'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      if: github.event_name == 'push'
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/cooked-backend:latest

    - name: Trigger Render Deploy
      if: github.event_name == 'push'
      run: |
         curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

  # Example Deployment to a VPS via SSH
  # deploy:
  #   needs: build
  #   if: github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Executing remote ssh commands to deploy
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.HOST }}
  #         username: ${{ secrets.USERNAME }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: |
  #           docker pull ${{ secrets.DOCKER_USERNAME }}/cooked-backend:latest
  #           docker stop cooked-backend || true
  #           docker rm cooked-backend || true
  #           docker run -d --name cooked-backend \
  #             -p 8099:8099 \
  #             --env-file /path/to/your/production.env \
  #             ${{ secrets.DOCKER_USERNAME }}/cooked-backend:latest
